// afk/raid.js
// Flow Raid cho Bit Heroes Auto

(function (global) {
    'use strict';

    const BH = global.__BH__ = global.__BH__ || {};
    BH.AFK = BH.AFK || {};
    BH.AFK.activities = BH.AFK.activities || {};

    BH.AFK.activities.raid = {
        name: 'Raid',
        enabled: true,
        priority: 4,

        open: [],

        // =========================================================
        // SETUP — 5 nút chạy tuần tự
        // =========================================================
        setup: [
            {
                x: 68,
                y: 342,
                hex: '#b400a9',
                tol: 15,
                waitAfter: 2000,
                label: 'Icon Raid'
            },
            {
                x: 682,
                y: 226,
                hex: '#a72f8f',
                tol: 15,
                waitAfter: 1000,
                label: 'Summon loại Raid'
            },
            {
                x: 816,
                y: 376,
                hex: '#f49341',
                tol: 15,
                waitAfter: 1000,
                label: 'Chọn độ khó'
            },
            {
                x: 738,
                y: 72,
                hex: '#9cd01f',
                tol: 15,
                waitAfter: 30000,
                label: 'Xác nhận / Bắt đầu',
                waitForNext: true,
                waitForNextTimeout: 120000
            },
            {
                // Nút Rerun — có 2 màu
                x: 410,
                y: 62,
                hex: '#a6d339',
                hexAlt: '#cbf067',       // Màu thay thế
                tol: 15,
                waitAfter: 2000,
                label: 'Rerun'
            }
        ],

        loop: [],
        loopCount: 0,
        close: [],

        noCloseOnTicket: true
    };

})(window);